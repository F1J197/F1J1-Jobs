# 18 — Persist Listings and deploy

**What to build:** Live app reads and writes Listings through Prisma on Neon, public HTTPS URL.

**Blocked by:** None — can start immediately

**Status:** ready-for-human

- [x] Prisma ListingCatalog mapper exists
- [ ] ListingPost persist to Job
- [ ] Neon `DATABASE_URL` in host env
- [ ] Vercel (or equal) + custom domain + HTTPS
- [ ] InMemory remains the test adapter
