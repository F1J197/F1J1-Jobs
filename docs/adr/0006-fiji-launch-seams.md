# Town matching, pay honesty, Interest, Report, Share, Events, and Watch are product seams

ListingCatalog still owns search. Town synonyms and Division aliases live inside that search — pages do not decide that Denarau is Nadi.

ListingPost still owns publish. Structured FJD pay, the FJD 5.00/hour floor, start date, and +679 WhatsApp live there.

New seams, tested the same way:

- **InterestLog** — `record` / `listByEmployer`. No message body, no Seeker phone. Rate-limit per actor + Listing.
- **ListingReport** — `submit` with a closed reason list.
- **ListingShare** — Facebook sharer and WhatsApp *share* (no Employer number). ExpressInterest remains the apply path.
- **EventLog** — first-party funnel counts. No third-party tracker.
- **AlertWatch** — Town/Category Watch; `matchingNew` uses ListingCatalog live rules + Town clusters.

**Considered options:** put Town and pay rules in pages; buy WhatsApp Business and GA. Pages would fork the domain. Paid rails wait until supply is real.
